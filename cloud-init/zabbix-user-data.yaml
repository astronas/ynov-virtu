#cloud-config
hostname: zabbix
manage_etc_hosts: true
package_update: true
packages:
  - qemu-guest-agent
  - curl
  - gnupg
  - nginx
  - php-fpm
  - mariadb-client
runcmd:
  - systemctl enable --now qemu-guest-agent

