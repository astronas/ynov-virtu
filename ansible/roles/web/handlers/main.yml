---
- name: Recharger nginx
  ansible.builtin.service:
    name: nginx
    state: reloaded

- name: Redemarrer PHP-FPM
  ansible.builtin.service:
    name: "{{ php_fpm_service }}"
    state: restarted
