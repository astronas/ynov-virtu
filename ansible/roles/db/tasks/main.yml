---
- name: Installer MariaDB
  ansible.builtin.apt:
    name:
      - mariadb-server
      - mariadb-client
    state: present
    update_cache: true

- name: Activer MariaDB
  ansible.builtin.service:
    name: mariadb
    state: started
    enabled: true

